# Quick Reference Commands

## 🛡️ Privacy Shield - Before Every Commit

```bash
# Quick scan for exposed secrets
git diff --cached | grep -E "(AIza|api.*key|secret|password)" -i

# If found, stop immediately and remove the file from staging:
git reset HEAD <filename>
```

## 📝 Standard Commit Workflow

```bash
# 1. Check what changed
git status

# 2. Review your changes
git diff

# 3. Stage files
git add <files>

# 4. Privacy check!
git diff --cached | grep -i "AIza"

# 5. Commit with conventional format
git commit -m "feat: your feature description"

# 6. Push (if ready)
git push origin master
```

## 🚀 Common Commit Messages for This Project

```bash
# New features
git commit -m "feat: add task filtering by category"
git commit -m "feat: implement keyboard shortcuts"

# Bug fixes  
git commit -m "fix: resolve localStorage data corruption"
git commit -m "fix: correct API error handling"

# UI/Style changes
git commit -m "style: improve mobile responsiveness"
git commit -m "style: update color scheme for accessibility"

# Documentation
git commit -m "docs: update README with new features"
git commit -m "docs: add API troubleshooting guide"

# Refactoring
git commit -m "refactor: extract API calls into separate module"
git commit -m "refactor: simplify task categorization logic"

# Maintenance
git commit -m "chore: update .gitignore for build files"
git commit -m "chore: upgrade to Gemini 2.5 Flash"
```

## 📋 Pre-Commit Checklist (Copy This!)

```
Before committing, verify:

[ ] No API keys in code (search for "AIza", "api_key", etc.)
[ ] Code tested in browser
[ ] No console.log() statements left in production code
[ ] README updated if features changed
[ ] Commit message follows conventional commits format
[ ] Files staged are correct (git status)
[ ] No sensitive data in changes (git diff --cached)
```

## 🔧 Useful Git Commands

```bash
# View changes not yet staged
git diff

# View changes staged for commit
git diff --cached

# Unstage a file
git reset HEAD <filename>

# Amend last commit (before pushing!)
git commit --amend -m "new message"

# View commit history with details
git log --graph --decorate --oneline

# Check what's ignored
git status --ignored

# Discard changes in a file
git checkout -- <filename>
```

## 🏷️ Commit Message Types Quick Reference

| Type | Use For | Example |
|------|---------|---------|
| `feat:` | New feature | "feat: add voice input" |
| `fix:` | Bug fix | "fix: resolve memory leak" |
| `docs:` | Documentation | "docs: update README" |
| `style:` | Formatting, CSS | "style: fix button alignment" |
| `refactor:` | Code restructuring | "refactor: split large function" |
| `perf:` | Performance | "perf: optimize API calls" |
| `test:` | Tests | "test: add unit tests" |
| `chore:` | Maintenance | "chore: update dependencies" |
